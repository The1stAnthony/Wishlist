# Copy this file to .env and fill in your own values.
# NEVER commit .env to git — it is already in .gitignore.

PORT=5000
NODE_ENV=development

# ── JWT ────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace_with_a_long_random_secret_at_least_32_chars

# ── Database ────────────────────────────────────────────────
# Supabase → Project Settings → Database → Connection string (URI)
# Development: use direct connection port 5432
# Production on Vercel: use Transaction mode pooler port 6543
DATABASE_URL=postgresql://postgres:[YOUR-DB-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# ── Amazon Associates ───────────────────────────────────────
# Apply at: affiliate-program.amazon.com
# Format: yourstore-20  (get your own — never use someone else's tag)
AMAZON_AFFILIATE_TAG=your-store-id-20

# ── Google AdSense ──────────────────────────────────────────
# Your publisher ID from AdSense → Account → Account information
# NOTE: this ID is also embedded in client/index.html for the AdSense script tag
ADSENSE_CLIENT_ID=ca-pub-XXXXXXXXXXXXXXXX

# ── Email (nodemailer) ──────────────────────────────────────
# Used for: password reset emails + contact form submissions
# Gmail: use an App Password (Google Account → Security → 2FA → App passwords)
# If not set, reset tokens are logged to the console (dev mode only)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=your-gmail@gmail.com
MAIL_PASS=your-gmail-app-password
CONTACT_EMAIL=your-gmail@gmail.com   # Where contact form submissions land

# ── App ─────────────────────────────────────────────────────
# Full public URL, used in password reset email links
APP_URL=https://alliwant.xyz

# ── Platform creator handle ─────────────────────────────────
# The display_name of the platform admin account in the DB.
# New creators auto-follow this account when they enable Creator Mode.
PLATFORM_CREATOR_HANDLE=The_1st_Anthony
